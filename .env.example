# Placeholders only — never fill in real secrets here.
BEE_URL=http://localhost:1633
# Archive publishing private key (hex or 0x-prefixed). Leave empty.
PRIVATE_KEY=
# Postage batch id used for uploads and feed writes. Leave empty.
BATCH_ID=
# Directory where recovered files are written.
OUTPUT_DIR=./recovered